"use client";
import { Graph } from "@/entities/graph";
import { DraggableList } from "@/features/draggable-list";
const Home = () => {
  return (
    <div>
      <DraggableList />
      {/* <Graph /> */}
    </div>
  );
};

export default Home;
